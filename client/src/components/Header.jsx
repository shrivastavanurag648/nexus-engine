import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '1rem', borderBottom: '1px solid var(--border)' }}>
            <h1 className="gold-text" style={{ margin: 0 }}>Hostel Allocation System</h1>
        </header>
    );
};

export default Header;
